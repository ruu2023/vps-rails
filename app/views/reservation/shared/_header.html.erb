<header class="sticky top-0 z-10 flex items-center justify-between bg-white px-4 py-4 shadow-md">
  <%= render "shared/app_switcher", current: :reservation, label: "予約カレンダー", title_class: "text-xl font-semibold text-gray-900" %>
  <div class="flex items-center gap-2">
    <%= render "shared/user_menu" %>
  </div>
</header>
